<h2><%= author.name %></h2>
<a href="/authors/<%= author.id %>">Edit</a>
<%- include('../partials/deleteForm',
 {url : `/authors/${author.id}` }) %>
 <% if(bookByAuthor.length > 0) { %>
    <h2>Books By Authors</h2>
    <% bookByAuthor.forEach(book => { %>
        <img height="150" width="100" src="<%= book.coverImagePath %>">
    <% }) %> 
<% } %>